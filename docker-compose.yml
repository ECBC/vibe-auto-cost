version: '3.8'

services:
  vibe_agent:
    build: .
    container_name: vibe_agent_container
    volumes:
      - .:/workspace
    env_file:
      - .env
    stdin_open: true
    tty: true
    restart: unless-stopped